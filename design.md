# 本地短时活动规划与执行 Agent 设计文档

## 1. 目标与比赛定位
本作品面向“本地 4-6 小时活动规划 + 关键动作推进”场景，强调从传统搜索/推荐升级为任务完成型 Agent。用户输入一句自然语言目标后，系统不仅生成综合方案，还会检查本地候选、计算路线时长、组织执行动作，并输出可分享结果。

比赛版重点不是全真交易，而是：
- 真实本地能力尽量真：位置、POI、路线、通勤时间
- 执行闭环足够可信：预约、排号、下单、配送、分享有明确动作反馈
- 演示链路完整稳定：无 Key 或接口异常时自动回退到 Mock，不中断整体展示

## 2. Planning 策略
规划流程分四层：

1. 目标理解
- `GoalParser` 将一句话目标转为结构化 `Goal`
- 识别场景、人数、时段、时长、距离偏好、儿童年龄、餐饮偏好、出发点、城市、出行方式

2. 候选获取
- `MockToolbox` 优先走高德真实本地能力
- 若存在 `AMAP_WEB_SERVICE_KEY`，则基于当前位置或城市检索活动、餐厅、补充活动
- 若不存在 Key 或请求失败，则自动回退到本地 Mock 数据

3. 方案评分
- `Planner` 对候选和 itinerary 两层打分
- 评分考虑：
  - 路线效率
  - 人群适配
  - 体验丰富度
  - 性价比
  - 执行稳定性
- 输出主推荐方案和备选方案
- 每条方案都保留评分拆解、推荐理由和规划依据，便于比赛展示和答辩说明

4. 执行动作编排
- 主方案会被转换为结构化 `ExecutionAction`
- 家庭场景默认更偏 `reserve + queue + delivery + share`
- 朋友场景默认更偏 `reserve + queue + order + share`

## 3. Tool 调用链路
当前作品采用“真实优先，Mock 兜底”的 Tool 结构：

- 查询类 Tool
  - `search_activities()`
  - `search_restaurants()`
  - `search_addons()`
  - `check_availability()`
  - `estimate_transition()`

- 执行类 Tool
  - `reserve()`
  - `queue()`
  - `order()`
  - `delivery()`
  - `share()`

- 编排关系
  - `api.py:/plan` 接收目标与位置上下文
  - `LocalActivityAgent.plan()` 调 `GoalParser` 与 `Planner`
  - `Planner` 调查询类 Tool 获取候选、计算路线、生成主备方案与动作
  - `api.py:/execute` 接收确认后的规划结果
  - `Executor` 顺序执行动作，失败后触发补偿动作
  - `ShareComposer` 输出适合家人或朋友直接转发的摘要

## 4. 异常处理机制
比赛版要求“始终可演示”，因此异常处理遵循双层兜底：

### 数据层兜底
- 无 `AMAP_WEB_SERVICE_KEY`
  - 自动回退到 Mock 候选，不阻断规划
- 高德接口超时或失败
  - 自动使用本地 Mock 结果继续输出方案

### 执行层兜底
- `activity_unavailable`
  - 主活动不可用时切换备选活动并重排
- `restaurant_busy`
  - 输出较长等待提醒，仍保留可执行方案
- `availability_timeout`
  - 跳过不可确认候选，优先保留稳定组合
- `reserve_timeout`
  - 触发备选时段重试
- `partial_failure`
  - 把排号或配送失败转成先点单或到店自取等补偿动作

异常策略的设计原则：
- 不回滚已成功动作
- 优先局部补偿，而不是整条方案重算
- 前端必须显式展示失败原因和补偿动作

## 5. 评审维度对齐
### 创新性
- 从“推荐几个地点”升级为“理解场景 -> 组织多品类供给 -> 执行动作推进”
- 前端显式展示规划阶段和需求标签，让 Agent 逻辑可见，而不是只给结果

### 完整性
- 覆盖输入、规划、路线、本地候选、执行、补偿、分享的完整闭环
- 新版 Web 界面补上主备方案对比、评分依据、推荐理由和待执行动作预览

### 应用效果
- 真实模式下使用高德位置与 POI
- 无真实 Key 时仍能稳定输出降级结果
- 方案不只返回点位列表，而是展示统一评分后的主推荐和备选路线

### 商业价值
- 提升用户从“搜索比对”到“直接安排完成”的效率
- 强化玩、吃、配送、分享等跨品类联动能力

## 6. 参考反馈后的改进重点
- 老师反馈强调的三个问题是：真实数据、需求标签、评分说服力
- 当前版本已对应补强：
  - 真实数据：高德检索与路线优先，失败时 Mock 兜底
  - 需求标签：把场景、人数、时长、距离、节奏、饮食和特殊需求显式展示
  - 评分说服力：给出主备方案对比和拆解后的评分依据
