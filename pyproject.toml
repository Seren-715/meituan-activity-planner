[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "meituan-demo"
version = "0.1.0"
description = "本地短时活动规划与执行 Agent CLI Demo"
requires-python = ">=3.10"

[project.scripts]
meituan-demo = "meituan_demo.cli:main"
